#include "holberton.h"
/**
 * int _isupper - checks character for case
 * @c: character to be checked
 * Return: 1 if upper, 0 if lower
 **/
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		_putchar('1')
	else
		_putchar('0')
}
