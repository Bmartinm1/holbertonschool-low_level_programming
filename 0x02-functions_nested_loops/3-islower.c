#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
* main - program entry point
*
* Return: always 0
**/
int _islower(int c)
{
	char ch = 'c';

	if (islower(ch))
	{
		return (1);
	} else
	{
		return (0);
	}
}
