#include <stdio.h>

/**
 * main - entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char myStr="Programming is like building a multilingual puzzle\n";
	int puts(myStr);

	return(0);
}
